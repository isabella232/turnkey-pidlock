#!/usr/bin/env python3

from distutils.core import setup

setup(name='turnkey-pidlock',
    version='1.0',
    description='A small filelocking library',
    author='Stefan Davis',
    url='https://github.com/turnkeylinux/turnkey-pidlock',
    packages=['pidlock'])
